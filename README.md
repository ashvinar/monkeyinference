# monkeyinference

This `main` branch is a seed so the engine can land as a pull request.

The complete Metal/MLX runtime and the measurement record live on
`cursor/ternary-metal-engine-09ea`. Merge that PR for the runnable tree and
`docs/ternary-engine.md`.
